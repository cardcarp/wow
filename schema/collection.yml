name:
  type: string
  required: true

type: # capitalize
  type: string
  required: true
  allowed: ['Set', 'Raid', 'Promo', 'Starter', 'Token', 'Extra']

index: # release order across all collections
  type: string
  nullable: true
  coerce: str
