name:
  type: string
  required: true

type:
  type: string
  required: true
  allowed: ['set', 'raid', 'promo', 'starter', 'token', 'extra']

index:
  type: string
  nullable: true
  coerce: str

publisher:
  type: string
  allowed: ['Blizzard', 'reborn']
